#  Python artefacts
__pycache__/
*.py[cod]
*.pyd
*.pyo
*.so
*.dylib
*.egg-info/
dist/
build/

#  Editor / OS clutter
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/

#  Virtual‑/Conda environments created locally or on the cluster
venv/
.venv/
/envs/                     # envs/ under $SCRATCH/tengan_runs (HPC)
/tengan_runs/envs/         

#  Jupyter artefacts
.ipynb_checkpoints/

#  Runtime outputs ─ TenGAN & HPC jobs
# generic logs and artefacts
*.log
*.out
*.err

# Lightning & TensorBoard
lightning_logs/

# TenGAN result folders (created inside repo if someone runs locally)
res/
runs*/                     # any runs/, runs_local/, runs_remote/ …

# HPC job directories (never push!)
tengan_runs/
jobs*/

# Model checkpoints & pickles
*.ckpt
*.pkl
*.pt
*.sav

# Plots generated by plot_results.py or notebooks
*.png
*.pdf
plots/

#  Data
# Full or sensitive datasets (keep only tiny examples in Git)
data/*.csv
data/*.sdf
data/*.smi
data/*.json
!data/combined_fuel_sample.csv     # ← keep the 100‑line example
!data/gaussian_templates/**        # keep small Gaussian templates

#  Gaussian large outputs (keep templates, ignore heavy files)
*.chk
*.fchk
*.gjf
*.com

#  Environment snapshots we WANT to keep
# Keep YAML exports (env/tengan_env.yml, env.yml inside run folders)
!env/**/*.yml
!*/env.yml

